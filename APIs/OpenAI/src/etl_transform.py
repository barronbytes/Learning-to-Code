class Transform():
    @staticmethod
    def validate(raw_data: list[str]) -> bool:
        extraction_errors = ["Extraction Error: no data found or null data"]

    def brain(raw_data: list[str]):
        return Transform.validate(raw_data)